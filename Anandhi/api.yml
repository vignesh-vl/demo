
kind: api
apiVersion: api.webmethods.io/beta
metadata:
  name: Aadhir
  namespace: viki
  version: alpha
  tags:
    - onei
    - tw
  
  type: REST
spec:
  mocked: true
  api-spec: 
    $path: petstore.yaml
  policy-sequence: 
    - $ref: daso:policyseq:alpha
