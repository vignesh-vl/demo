
kind: api
apiVersion: api.webmethods.io/beta
metadata:
  name: Aadhira
  namespace: viki
  version: alpha
  tags:
    - one
    - tw
    - three
  type: REST
spec:
  mocked: true
  api-spec: 
    $path: petstore.yaml
  policy-sequence: 
    - $ref: daso:policyseq:alpha